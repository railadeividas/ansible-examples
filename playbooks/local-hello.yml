---
- name: Print output
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:

    - debug:
        msg: hello world

    - name: Force all handlers to run at this point, not waiting for normal sync points
      ansible.builtin.meta: flush_handlers

    - name: End role
      ansible.builtin.meta: end_role
